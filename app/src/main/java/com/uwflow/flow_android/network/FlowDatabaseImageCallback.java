package com.uwflow.flow_android.network;

import com.uwflow.flow_android.db_object.ScheduleImage;

public interface FlowDatabaseImageCallback {
    public void onScheduleImageLoaded(ScheduleImage image);
}
